//////////////////////////////////////////////////////////////////////
//	created:	2013/02/13
//	filename:	src\BSLib\utility\memory\new.cpp
//	author:		mazhejiang
//	
///	purpose:	
//////////////////////////////////////////////////////////////////////
#include <BSLib/utility/memory/memory.h>
#include <BSLib/utility/memory/new.h>
/*
void* operator new(Base::uint32 a_size)
{
	return Base::Utility::CMemory::allocateMemory(a_size);
}

void operator delete(void* a_data)
{
	Base::Utility::CMemory::deallocateMemory(a_data);
}

void* operator new[](Base::uint32 a_size)
{
	return Base::Utility::CMemory::allocateMemory(a_size);
}

void operator delete[](void* a_data)
{
	Base::Utility::CMemory::deallocateMemory(a_data);
}

void* operator new(Base::uint32 a_size, const char* fileName, int line)
{
	return Base::Utility::CMemory::allocateMemory(a_size, fileName, line);
}

void operator delete(void* a_data, const char* fileName, int line)
{
	Base::Utility::CMemory::deallocateMemory(a_data, fileName, line);
}

void* operator new[](Base::uint32 a_size, const char* fileName, int line)
{
	return Base::Utility::CMemory::allocateMemory(a_size, fileName, line);
}

void operator delete[](void* a_data, const char* fileName, int line)
{
	Base::Utility::CMemory::deallocateMemory(a_data, fileName, line);
}*/


